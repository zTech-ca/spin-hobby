import { ECurrencies } from "../enum";

export const baseCurrency = ECurrencies.CAD;

export const availableCurrencyConversions = [
  ECurrencies.CAD,
  ECurrencies.JPY,
  ECurrencies.USD,
  ECurrencies.EUR,
  ECurrencies.KRW,
  ECurrencies.CNY,
  ECurrencies.HKD,
];
